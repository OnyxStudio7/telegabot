# 🎰 Telegram Slot Bot (Template)

Готовый шаблон Telegram-бота со встроенным HTML5-слотом.

## Что внутри
- `web/index.html` — HTML5 слот (взято из загруженного файла).
- `bot/` — Python-бот (aiogram) + FastAPI API для баланса/спинов.
- `.gitignore`, `README.md`, `vercel.json`.

## Установка (локально)
1. Склонируй репозиторий.
2. Перейди в папку `bot`:
   ```bash
   cd bot
   pip install -r requirements.txt
   ```
3. Создай файл `.env` рядом с `bot/main.py`:
   ```
   BOT_TOKEN=123456:ABC-DEF...
   WEB_URL=https://username.github.io/telegram-slot-bot/
   ```
4. Запуск:
   ```bash
   python main.py
   ```
   После запуска бот будет слушать Telegram и одновременно поднят HTTP API на порту 8000 (по умолчанию).

## Развёртывание
- Веб (HTML) можно разместить на **GitHub Pages** или **Vercel** (папка `/web`).
- Бота/API можно разворачивать на **Render**, **Railway**, **Fly.io** или на VPS. Убедись, что переменные окружения (`BOT_TOKEN`) заданы.

## Полезно
- `bot/database.json` содержит сохранённые балансы пользователей (локально).
- В `web/index.html` реализовано обращение к API (если открыт как WebApp в Telegram, доступно `window.Telegram.WebApp`).

Удачи! 🚀
